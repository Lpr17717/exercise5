import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  base: '/projects/project-2',
  build: {
    site: 'http://larissarogozin.com/projects/project-2/',
  }
});